/**
 * @version    1.6.1.1
 * @build      7-9-2020
 * @package    NowaDays - Multipurpose Creative Theme
 * @author     Pavel Marhaunichy <help@likeaprothemes.com>
 * @license    https://themeforest.net/licenses
 *
 * @website    https://likeaprothemes.com
 */

!(function (t) {
  "use strict";
  function e(e, i) {
    var e = e || 800,
      n = t(".p-preloader__top, .p-preloader__bottom");
    return n.length
      ? void n.smoothAnimate(
          { height: ["50%", 0] },
          { duration: e, complete: i }
        )
      : void i();
  }
  function i() {
    var t = window,
      e = "inner";
    return (
      "innerWidth" in window || ((t = K), (e = "client")),
      (L = t[e + "Width"]),
      (Q = t[e + "Height"]),
      { width: L, height: Q }
    );
  }
  function n() {
    H = document.documentElement.scrollTop || document.body.scrollTop;
  }
  function o() {
    U = t(document).height();
  }
  function s(t) {
    return t > 0 && t < 1 ? 0 : Math.ceil(t);
  }
  function a() {
    return Y.hasClass("p-onepage")
      ? void ($ = !0)
      : void (Y.hasClass("p-multipage") && (W = !0));
  }
  function r() {
    return !!Y.hasClass("p-asidemenupage");
  }
  function l() {
    return !!Y.hasClass("menu-mobile-hidden");
  }
  function c() {
    return L < 768;
  }
  function u() {
    if (t.platform.ios) {
      Y.addClass("ios-device");
      var e = document.querySelector(".portfolio-item");
      e && (t(e).parents(".section")[0].onclick = function () {});
    }
  }
  function d() {
    return !!Y.hasClass("sticky-header");
  }
  function h() {
    X.hasClass("header-sticky-relative") && (V = !0);
  }
  function m() {
    var e = document.getElementById("footer-twitter-feed-vertical__list");
    if (null !== e) {
      var i = {
        profile: { screenName: e.getAttribute("data-twitter-id") },
        id: e.getAttribute("data-twitter-id"),
        domId: "footer-twitter-feed-vertical__list",
        maxTweets: +e.getAttribute("data-twitter-max-tweets") || 3,
        enableLinks: !0,
        showPermalinks: !1,
        showUser: !1,
        showTime: !1,
        dateFunction: "",
        showInteraction: !1,
        customCallback: function (t) {
          for (var i = "", n = 0; n < t.length; n++)
            i +=
              '<li class="footer-twitter-feed-vertical__item ico-119">' +
              t[n] +
              "</li>";
          e.innerHTML = i;
        },
      };
      twitterFetcher.fetch(i),
        (function n() {
          setTimeout(function () {
            t(".footer-twitter-feed-vertical__item").length
              ? t(window).trigger("resize")
              : n();
          }, 100);
        })();
    }
  }
  function f(e, i) {
    function n() {
      var t = new Image();
      return (
        (t.onload = function () {
          e.setAttribute("data-w", this.naturalWidth),
            e.setAttribute("data-h", this.naturalHeight),
            o();
        }),
        (t.onerror = function () {
          console && console.log("something wrong with image: " + t.src), o();
        }),
        t
      );
    }
    function o() {
      i && i();
    }
    return t(e).length
      ? void ("img" === e.tagName.toLowerCase()
          ? (n().src = e.src)
          : "none" !== getComputedStyle(e).backgroundImage &&
            "" !== getComputedStyle(e).backgroundImage
          ? (n().src = getComputedStyle(e).backgroundImage.replace(
              /url\(|\)$|"/gi,
              ""
            ))
          : o())
      : (o(), !1);
  }
  function p() {
    function e() {
      o.each(function () {
        t(this).slideLikeAPro();
      });
    }
    function i() {
      function e() {
        o.removeClass("hidden").animate({ width: "100%" }, 1200);
      }
      function i() {
        a.stop(!0).animate({ width: 0 }, 600, function () {
          t(this).addClass("hidden"), e();
        });
      }
      var n,
        o,
        a = t(".main-5-slider-text__subtitle"),
        r = !1;
      s.on("initialized.owl.carousel changed.owl.carousel", function (i) {
        var s = i.item.index,
          a = t(".main-5-slider-text").eq(s);
        (n = a.find(".main-5-slider-text__title")),
          (o = a.find(".main-5-slider-text__subtitle")),
          s < 0 && (s = 0),
          0 === s &&
            n
              .removeClass("hidden")
              .delay(800)
              .animate({ width: "100%" }, 1800, function () {
                e();
              }),
          s + 1 !== i.item.count ||
            r ||
            (setTimeout(function () {
              t(".section-arrow").css("display", "block");
            }, 1500),
            (r = !0));
      });
      var l = {
          loop: !1,
          autoplay: !0,
          video: !1,
          items: 1,
          smartSpeed: 700,
          autoplayTimeout: 4e3,
          nav: !1,
          dots: !0,
          dotsClass: "main-5-slider-dots",
          dotClass: "main-5-slider-dots__dot",
          controlsClass: "main-5-slider-controls",
          navText: "",
          navClass: ["slider-arrow-prev ico-111", "slider-arrow-next ico-112"],
          onChange: i,
          URLhashListener: !1,
        },
        c = s.data("owl"),
        u = t.extend(l, c);
      s.owlCarousel(u);
      var d = s.find(".slider-arrow-prev"),
        h = s.find(".slider-arrow-next");
      s.mouseenter(function () {
        d.add(h).addClass("animated");
      }).mouseleave(function () {
        d.add(h).removeClass("animated");
      });
    }
    function n(t) {
      t.addEventListener("click", function (e) {
        matchesSelector(e.target, ".portfolio-filter__item") &&
          (t
            .querySelector(".portfolio-filter__item_active")
            .classList.remove("portfolio-filter__item_active"),
          e.target.classList.add("portfolio-filter__item_active"));
      });
    }
    var o = t(".slide-like-a-pro");
    o.length && e();
    var s = t(".main-5-slider");
    s.length && i(),
      Z.isotope({ itemSelector: '[class*="col-"]' }),
      Z.find(".portfolio-item__img").on("load", function () {
        Z.isotope("layout");
      });
    var a = document.querySelector(".portfolio-filter"),
      r = Z.find("[data-category]"),
      l = {};
    r.each(function () {
      for (
        var e = this.getAttribute("data-category")
            .toLowerCase()
            .trim()
            .split(/\s*,\s*/),
          i = 0;
        i < e.length;
        i++
      )
        t(this)
          .parent('[class*="col-"]')
          .addClass("portfolio-filter-" + e[i].replace(/\s/g, "-")),
          l[e[i]] || (l[e[i]] = e[i]);
    });
    var c = "";
    for (var u in l)
      l.hasOwnProperty(u) &&
        (c +=
          '<span class="portfolio-filter__item" data-filter=".portfolio-filter-' +
          u.replace(/\s/g, "-") +
          '">' +
          u +
          "</span>");
    t(a).append(c),
      a &&
        (a.addEventListener("click", function (e) {
          if (matchesSelector(e.target, ".portfolio-filter__item")) {
            t(window).trigger("resize");
            var i = e.target.getAttribute("data-filter");
            Z.isotope({ filter: i }, !1);
          }
        }),
        n(a));
    var d = t(".portfolio-carousel");
    if (d.length) {
      var h = d.attr("data-portfolio-carousel-items");
      d.owlCarousel({
        loop: !1,
        nav: !1,
        dots: !1,
        margin: 0,
        responsive: {
          0: { items: 1 },
          480: { items: 2 },
          768: { items: h >= 3 ? 3 : h },
          992: { items: h >= 4 ? 4 : h },
          1200: { items: h },
        },
        onInitialized: function () {
          this.$stage[0].style.width =
            Math.ceil(this.$stage[0].offsetWidth) + 1 + "px";
        },
        onResized: function () {
          t(window).resize(),
            (this.$stage[0].style.width =
              Math.ceil(this.$stage[0].offsetWidth) + 1 + "px");
        },
      });
    }
    var m = {
      numbersEl: document.getElementsByClassName("counters-item__count"),
      linearBar: document.getElementsByClassName("skills-graph-linear__bar"),
      linearCount: document.getElementsByClassName(
        "skills-graph-linear__count"
      ),
      circleEl: document.getElementsByClassName("skills-graph-circle"),
      circleBar: document.getElementsByClassName("skills-graph-circle__bar"),
      circleCount: document.getElementsByClassName(
        "skills-graph-circle__count"
      ),
      init: function () {
        var e = this;
        e.numbersEl.length &&
          t(e.numbersEl).one("animationOnScrollStarted", function () {
            e.numbers(this, t(e.numbersEl).index(this));
          }),
          e.linearBar.length &&
            t(e.linearBar).one("animationOnScrollStarted", function () {
              e.linear(this, t(e.linearBar).index(this));
            }),
          e.circleBar.length &&
            t(e.circleEl).one("animationOnScrollStarted", function () {
              e.circle(this, t(e.circleEl).index(this));
            });
      },
      numbers: function (e, i) {
        function n(t) {
          if (!l.length) return t;
          for (; /(\d+)(\d{3})/.test(t.toString()); )
            t = t.toString().replace(/(\d)(?=(\d\d\d)+(?!\d))/g, "$1" + l);
          return t;
        }
        var o = this.numbersEl[i],
          s = parseInt(o.getAttribute("data-counters-number"), 10),
          a = o.getAttribute("data-counters-units") || "",
          r = o.getAttribute("data-counters-units-after") || "true",
          l = o.getAttribute("data-counters-delimiter") || "",
          c = parseInt(o.getAttribute("data-counters-duration"), 10) || 3600;
        t(e).smoothAnimate(
          { value: [0, s] },
          {
            easing: "easeOutQuart",
            duration: c,
            step: function (t, e, i) {
              var s = "true" === r ? n(Math.ceil(i)) + a : a + n(Math.ceil(i));
              o.textContent = s;
            },
          }
        );
      },
      linear: function (e, i) {
        var n = this.linearCount[i],
          o = parseInt(n.getAttribute("data-skills-graph-perc"), 10);
        t(e).smoothAnimate(
          { width: [0, o + "%"] },
          {
            easing: "easeInOutQuad",
            duration: (100 * o) / 6,
            step: function (t, e, i) {
              n.textContent = Math.ceil(i);
            },
            complete: function () {}.bind(this),
          }
        );
      },
      circle: function (e, i) {
        var n = this.circleBar[i],
          o = n.getAttribute("r"),
          s = 2 * Math.PI * o,
          a = this.circleCount[i],
          r = parseInt(a.getAttribute("data-skills-graph-perc"), 10);
        (n.style.strokeDasharray = n.style.strokeDashoffset = s),
          t(e).smoothAnimate(
            { value: [0, r] },
            {
              easing: "easeOutBack",
              duration: 3600,
              step: function (t, e, i) {
                (n.style.strokeDashoffset = s - (i * s) / 100),
                  (a.textContent = Math.ceil(i));
              },
              complete: function () {}.bind(this),
            }
          );
      },
    };
    m.init();
    var f = t(".testimonials");
    if (f.length) {
      var p = f.data("timeout");
      f.one("initialized.owl.carousel", function () {
        t(window).resize();
      }),
        f.owlCarousel({
          loop: !0,
          autoplay: !0,
          autoplayTimeout: p,
          items: 1,
          smartSpeed: 450,
          nav: !1,
          dots: !0,
          dotsClass: "testimonials-dots",
          dotClass: "testimonials-dots__dot",
          autoplayHoverPause: !0,
        });
    }
    var g = t(".client-logos-carousel");
    if (g.length) {
      var p = g.data("timeout");
      g.owlCarousel({
        loop: !0,
        autoplay: !0,
        autoplayTimeout: p,
        smartSpeed: 450,
        nav: !1,
        dots: !1,
        autoplayHoverPause: !0,
        responsive: {
          0: { items: 2 },
          480: { items: 3 },
          768: { items: 4 },
          992: { items: 5 },
        },
      });
    }
    var v = document.getElementsByClassName("tooltip-alt"),
      y = document.getElementsByClassName("tooltip");
    t(v).hover(
      function () {
        var t = this,
          e = t.getBoundingClientRect(),
          i = e.left + e.width / 2,
          n = e?.top - e.height / 2,
          o = t.getAttribute("alt");
        if (o) {
          var s = document.createElement("div");
          s.className = "tooltip";
          var a = document.body.appendChild(s);
          (a.style.display = "block"),
            (a.style.top =
              n - parseInt(getComputedStyle(a).height, 10) / 2 + "px"),
            (a.style.left =
              i - parseInt(getComputedStyle(a).width, 10) / 2 + "px"),
            (a.innerHTML = o),
            setTimeout(function () {
              try {
                document.body.removeChild(a);
              } catch (t) {
                return;
              }
            }, 2e3);
        }
      },
      function () {
        void 0 !== y[0] && document.body.removeChild(y[0]);
      }
    );
  }
  function g() {
    var e = t(".blog-slider");
    if (e.length) {
      e.owlCarousel({
        loop: !0,
        autoplay: !0,
        autoplayTimeout: 2e3,
        items: 1,
        smartSpeed: 450,
        dots: !1,
        nav: !0,
        navText: "",
        navClass: ["slider-arrow-prev ico-111", "slider-arrow-next ico-112"],
        autoplayHoverPause: !0,
      });
      var i = t(".slider-arrow-prev"),
        n = t(".slider-arrow-next");
      e.mouseenter(function () {
        i.add(n).addClass("animated");
      }).mouseleave(function () {
        i.add(n).removeClass("animated");
      });
    }
    (window.responsiveEmbedException = /spotify\.com.*track/),
      (function () {
        for (
          var e = document.querySelectorAll("iframe, object, embed"),
            i = t('<div class="responsive-embed"></div>'),
            n = 0;
          n < e.length;
          n++
        ) {
          var o = e[n].src || e[n].data;
          responsiveEmbedException.test(o)
            ? (e[n].removeAttribute("height"),
              e[n].removeAttribute("width"),
              (e[n].style.width = "100%"))
            : (e[n].setAttribute("data-aspect-ratio", e[n].height / e[n].width),
              t(e[n]).wrap(i));
        }
        t(window)
          .smartresize(function () {
            for (var i = 0; i < e.length; i++) {
              var n = e[i].src || e[i].data;
              if (!responsiveEmbedException.test(n)) {
                var o = t(e[i]).parents(".responsive-embed").width();
                e[i].setAttribute("width", o),
                  e[i].setAttribute(
                    "height",
                    o * e[i].getAttribute("data-aspect-ratio")
                  );
              }
            }
          })
          .resize();
      })(),
      t.getJSON(
        "https://graph.facebook.com/?id=" + location.href + "&callback=?",
        function (e) {
          t(".post-share__count_fb").attr("data-count-shares", e.shares),
            0 !== e.shares && t(".post-share__count_fb").text(e.shares);
        }
      );
    var o = {
      go: function (e, i) {
        var n,
          s = o,
          a = t.extend(
            {
              type: "fb",
              url: location.href,
              title: document.title,
              image: t(".post__img").attr("src"),
              text: t(".post-content").text(),
            },
            t(e).data(),
            i
          );
        return (
          null === s.popup((n = s[a.type](a))) &&
          (t(e).is("a")
            ? (t(e).prop("href", n), !0)
            : ((location.href = n), !1))
        );
      },
      fb: function (e) {
        var i = t(".post-share__count_fb").attr("data-count-shares"),
          n = Number(i) + 1;
        n == n && t(".post-share__count_fb").text(n);
        var o = t.extend(
          { url: location.href, title: document.title, image: "", text: "" },
          e
        );
        return (
          "https://www.facebook.com/sharer/sharer.php?u=" +
          encodeURIComponent(o.url) +
          "&t=" +
          encodeURIComponent(o.title)
        );
      },
      tw: function (e) {
        var i = t(".post-share__count_tw").attr("data-count-shares"),
          n = Number(i) + 1;
        n == n && t(".post-share__count_tw").text(n);
        var o = t.extend(
          { url: location.href, title: document.title, image: "" },
          e
        );
        return (
          "https://twitter.com/intent/tweet?text=" +
          encodeURIComponent(o.title) +
          "&url=" +
          encodeURIComponent(o.url)
        );
      },
      li: function (e) {
        var i = t(".post-share__count_li").attr("data-count-shares"),
          n = Number(i) + 1;
        n == n && t(".post-share__count_li").text(n);
        var o = t.extend(
          { url: location.href, title: document.title, image: "", text: "" },
          e
        );
        return (
          "https://www.linkedin.com/shareArticle?mini=true&url=" +
          encodeURIComponent(o.url) +
          "&title=" +
          encodeURIComponent(o.title) +
          "&summary=" +
          encodeURIComponent(o.title) +
          "&source=" +
          encodeURIComponent(o.image)
        );
      },
      popup: function (t) {
        return window.open(
          t,
          "",
          "toolbar=0,status=0,scrollbars=1,width=626,height=436"
        );
      },
    };
    t(document).on("click", ".post-share__item_fb", function () {
      o.go(this, { type: "fb" });
    }),
      t(document).on("click", ".post-share__item_tw", function () {
        o.go(this, { type: "tw" });
      }),
      t(document).on("click", ".post-share__item_li", function () {
        o.go(this, { type: "li" });
      });
  }
  function v() {
    requestAnimationFrame(y);
  }
  function y() {
    if (
      st.complete &&
      (n(),
      ot.makeParallax(),
      $ && (it.updateOffsets(), it.testForMenuChange()),
      it.headerObserve(),
      et)
    ) {
      var t = et.offsetHeight;
      "absolute" === getComputedStyle(et).position &&
        (H <= t ? X.css("padding-top", t + "px") : X.css("padding-top", ""));
    }
  }
  function b() {
    console && console.log("resize"),
      i(),
      n(),
      k(),
      o(),
      ot.updateOffsets(),
      ot.makeParallax(),
      nt && (nt.updateOffsets(), nt.makeAnimate()),
      ($ || W) && it.updateOffsets(),
      B(),
      I(),
      D();
  }
  function w(t, e) {
    if (t + e > H && t < H + Q) {
      var i;
      return (i =
        H >= t
          ? 100 - Math.ceil((100 * (H - t)) / e)
          : t < H + Q && t + e > H + Q
          ? 100 - Math.ceil((100 * (t + e - (H + Q))) / e)
          : 100);
    }
    return !1;
  }
  function _(t) {
    return {
      width: +t.getAttribute("data-w"),
      height: +t.getAttribute("data-h"),
    };
  }
  function C() {
    c() || r()
      ? (at.addClass("ico-111"), rt.removeClass("ico-112"))
      : (at.removeClass("ico-111"), rt.addClass("ico-112"));
  }
  function k() {
    x(),
      C(),
      A(),
      r()
        ? c()
          ? (Y.removeClass("menu-mobile-opened"),
            dt.css("margin-bottom", "auto"),
            ut.css({ position: "relative", width: "auto" }))
          : l
          ? (dt.css("margin-bottom", ut.height() + "px"),
            ut.css({ position: "fixed", width: "100%" }))
          : (Y.addClass("menu-mobile-opened"),
            dt.css("margin-bottom", ut.height() + "px"),
            ut.css({
              position: "fixed",
              width: "calc(100% - " + lt.width() + "px)",
            }))
        : dt.css("margin-bottom", ut.height() + "px"),
      c() ||
        r() ||
        !Y.hasClass("menu-mobile-opened") ||
        (t(".menu-mobile-social").remove(),
        t(".submenu").css("display", ""),
        t(".menu__link, .submenu__link").removeClass("menu__link_pressed"),
        Y.removeClass("menu-mobile-opened"));
  }
  function x() {
    c() || r()
      ? Y.addClass("menu-mobile").removeClass("menu-desktop")
      : Y.addClass("menu-desktop").removeClass("menu-mobile");
  }
  function A() {
    if ((c() && !r() && d() && X.addClass("header-sticky header-dark"), et)) {
      var t = et.offsetHeight;
      "fixed" === getComputedStyle(et).position
        ? X.css("padding-top", t + "px")
        : H <= t
        ? X.css("padding-top", t + "px")
        : X.css("padding-top", "");
    }
  }
  function I() {
    if (!(!G.length > 0)) {
      J = J && J.length ? J : [];
      for (var e = 0; e < G.length; e++)
        t(G[e].parentNode).hasClass("equal-cols-container") ||
          (t(G[e].parentNode).addClass("equal-cols-container"),
          J.push(G[e].parentNode));
      for (var i = 0; i < J.length; i++) {
        for (
          var n = J[i].querySelectorAll(".equal-cols"), o = 0, s = 0;
          s < n.length;
          s++
        ) {
          var a = n[s];
          a.style.height = "";
          var r = parseInt(getComputedStyle(a).height, 10);
          r > o && (o = r);
        }
        for (var s = 0; s < n.length; s++) n[s].style.height = o + "px";
      }
    }
  }
  function S(t, e, i, n) {
    var t = void 0 === t || t,
      e = e || 400,
      i = i || 0;
    t
      ? pt.fadeIn(e, function () {
          gt.fadeIn(i, function () {
            n && n();
          });
        })
      : gt.fadeIn(i, function () {
          n && n();
        });
  }
  function T(t, e) {
    var t = t || 0;
    "block" === gt.css("display") &&
      gt.fadeOut(t, function () {
        e && e();
      });
  }
  function E(e, i, n) {
    function o() {
      switch ((ft.append(e), i)) {
        case "image":
          t(e)
            .load(function () {
              T(200, function () {
                var t = mt.width(),
                  e = mt.height();
                mt.fadeIn(yt), P(t, e);
              });
            })
            .error(function () {
              T(200, function () {
                E("Something went wrong.\nPlease try again", "", !1);
              });
            });
          break;
        case "video":
          var n = t(".modal__video")[0],
            o = t(n).attr("src");
          t.platform.ios ||
          /.*youtube\.com.*/i.test(o) ||
          /.*vimeo\.com.*/i.test(o)
            ? T(200, function () {
                var t = 16,
                  e = 9;
                mt.fadeIn(yt), P(t, e);
              })
            : (n.oncanplay = function () {
                T(200, function () {
                  var t = n.videoWidth,
                    e = n.videoHeight;
                  mt.fadeIn(yt), P(t, e);
                });
              });
          break;
        case "custom":
          var s = mt.find(".project-modal-custom-code");
          s.css("display", "block");
          var a = mt.width(),
            r = mt.height();
          mt.fadeIn(yt), P(a, r);
        default:
          ft.wrapInner('<span class="modal__text"></span>');
          var a = mt.width(),
            r = mt.height();
          T(200, function () {
            mt.fadeIn(yt), P(a, r);
          });
      }
    }
    return (n = void 0 === n || n), M(), n ? void S(!0, 400, 0, o) : void o();
  }
  function M() {
    mt.css({
      display: "none",
      width: "auto",
      height: "auto",
      "line-height": "normal",
    }),
      ft.empty();
  }
  function O() {
    mt.add(pt).smoothAnimate(
      { opacity: 0, value: [1, 2] },
      {
        duration: yt,
        step: function (t, e, i) {
          e.style.transform = "scale(" + i + ")";
        },
        complete: function (t) {
          pt.css({ display: "none", transform: "scale(1)", opacity: 0.85 }),
            mt.css({
              display: "none",
              transform: "scale(1)",
              opacity: 1,
              width: "auto",
              height: "auto",
              "line-height": "normal",
            }),
            ft.empty(),
            vt.css("display", "none");
        },
      }
    ),
      T(),
      wt.add(bt).removeClass("animated").off("click.sliderModal");
  }
  function P(t, e) {
    var i = K.clientWidth,
      n = Q,
      o = i / n,
      s = t / e,
      a = 0.9;
    if (o >= s) {
      var r = Math.round(n * a),
        l = Math.round(r * s),
        c = Math.round((n - r) / 2),
        u = Math.round((i - l) / 2);
      mt.css({
        width: l + "px",
        height: r + "px",
        top: c + "px",
        left: u + "px",
        "line-height": r + "px",
      });
    } else {
      var l = Math.round(i * a),
        r = Math.round(l / s),
        c = Math.round((n - r) / 2),
        u = Math.round((i - l) / 2);
      mt.css({
        width: l + "px",
        height: r + "px",
        top: c + "px",
        left: u + "px",
        "line-height": r + "px",
      });
    }
    var d = mt.find("iframe");
    d.length && d.css({ width: l + "px", height: r + "px" });
  }
  function B() {
    if ("block" === mt.css("display")) {
      var t = mt.width(),
        e = mt.height();
      P(t, e);
    }
  }
  function q(e) {
    function i(i, o) {
      !(function () {
        l = [];
        for (var i = 0, n = 0; i < a; i++)
          e[i].removeAttribute("data-sliderModal-item"),
            t(e[i]).parents('[class*="col-"]').is(":visible") &&
              ((l[n] = e[i]),
              e[i].setAttribute("data-sliderModal-item", n),
              n++,
              (r = n));
      })(),
        vt.css("display", "block");
      var o = o || i.target,
        s = t(o).attr("href"),
        c = parseInt(t(o).attr("data-sliderModal-item"), 10);
      if (t(o).hasClass("preview-photo"))
        i.preventDefault(),
          E('<img class="modal__img" src="' + s + '">', "image");
      else if (t(o).hasClass("preview-video"))
        if (
          (i.preventDefault(),
          /.*youtube\.com.*/i.test(s) || /.*vimeo\.com.*/i.test(s))
        ) {
          var u =
            "true" === o.getAttribute("data-video-autoplay")
              ? "?autoplay=1"
              : "";
          E(
            '<iframe class="modal__video" src="' +
              s +
              u +
              '" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe>',
            "video"
          );
        } else {
          var u =
            "true" === o.getAttribute("data-video-autoplay") ? " autoplay" : "";
          E(
            '<video class="modal__video" controls preload="metadata"' +
              u +
              '><source src="' +
              s +
              '">Sorry, but your browser not support this format video</video>',
            "video"
          );
        }
      else if (t(o).hasClass("preview-custom")) {
        var d = t(o).siblings(".project-modal"),
          h = d.find(".project-modal-custom-code");
        if (h.length) {
          i.preventDefault();
          var m = t('<div class="modal__custom-code"></div>');
          h.clone().appendTo(m), E(m, "custom");
        }
      }
      n(c);
    }
    function n(e) {
      a <= 1 ||
        r <= 1 ||
        (j(t(l[e])),
        0 === e
          ? ((s = l[e + 1]),
            wt.one("click.sliderModal", function (t) {
              t.stopPropagation(), bt.off("click.sliderModal"), i(t, s);
            }),
            wt.addClass("animated"),
            bt.removeClass("animated"))
          : e == r - 1
          ? ((o = l[e - 1]),
            bt.one("click.sliderModal", function (t) {
              t.stopPropagation(), wt.off("click.sliderModal"), i(t, o);
            }),
            bt.addClass("animated"),
            wt.removeClass("animated"))
          : ((o = l[e - 1]),
            bt.one("click.sliderModal", function (t) {
              t.stopPropagation(), wt.off("click.sliderModal"), i(t, o);
            }),
            (s = l[e + 1]),
            wt.one("click.sliderModal", function (t) {
              t.stopPropagation(), bt.off("click.sliderModal"), i(t, s);
            }),
            bt.add(wt).addClass("animated")));
    }
    t(
      '<div style="position:fixed" class="slider-arrow-prev ico-111"></div><div style="position:fixed" class="slider-arrow-next ico-112"></div>'
    ).insertAfter(pt),
      (bt = t(".slider-arrow-prev")),
      (wt = t(".slider-arrow-next"));
    var o,
      s,
      a = e.length,
      r = a,
      l = [];
    !(function () {
      for (var t = 0; t < a; t++) e[t].setAttribute("data-sliderModal-item", t);
    })(),
      (function () {
        e.on("click", function (t) {
          i(t);
        });
      })();
  }
  function j(t) {
    var e = !1,
      i = !1;
    vt.off("click")
      .one("click", function () {
        t.next().clone().appendTo(ft);
      })
      .on("click", function () {
        if (!i)
          return (
            (i = !0),
            e
              ? void ft.find(".project-modal__inner").smoothAnimate(
                  { opacity: [1, 0] },
                  {
                    duration: 600,
                    complete: function () {
                      ft.find(".project-modal").smoothAnimate(
                        { height: ["100%", 0] },
                        {
                          duration: 600,
                          easing: "ease",
                          complete: function () {
                            (e = !1), (i = !1);
                          },
                        }
                      );
                    },
                  }
                )
              : void ft
                  .find(".project-modal")
                  .css("display", "block")
                  .smoothAnimate(
                    { height: [0, "100%"] },
                    {
                      duration: 600,
                      easing: "ease",
                      complete: function (t) {
                        t.find(".project-modal__inner").smoothAnimate(
                          { opacity: [0, 1] },
                          {
                            duration: 600,
                            complete: function () {
                              (e = !0), (i = !1);
                            },
                          }
                        );
                      },
                    }
                  )
          );
      });
  }
  function F() {
    var e = document.querySelectorAll(
        ".p-preloader__top, .p-preloader__bottom"
      ),
      i = document.querySelector(".p-preloader"),
      n = [];
    Array.prototype.forEach.call(
      document.querySelectorAll(
        ".header-logo__link, .menu__link, .submenu__link, .footer-menu__link"
      ),
      function (t, e) {
        var i = t.getAttribute("href");
        0 !== i.indexOf("#", 0) &&
          "" !== i &&
          "_blank" !== t.target &&
          n.push(t);
      }
    ),
      t(n).on("click", function (n) {
        if (
          (n.preventDefault(),
          (!c() && !r()) ||
            !t(this).hasClass("menu__link") ||
            !t(this).not(":only-child").length)
        ) {
          var o = this.getAttribute("href");
          return (
            t(e).css("height", "50%"),
            i
              ? void t(i)
                  .smoothAnimate(
                    { opacity: [0, 1] },
                    {
                      duration: 300,
                      complete: function () {
                        document.location.href = o;
                      },
                    }
                  )
                  .css("display", "block")
              : void (document.location.href = o)
          );
        }
      });
  }
  function N() {
    function e(t) {
      (i = document.createElement("link")),
        (i.rel = "stylesheet"),
        (i.href =
          "assets/css/theme-colors/theme-" + t.replace("#", "") + ".css");
    }
    var i,
      n = t(".customizer__button"),
      o = document.getElementsByTagName("head")[0];
    n.on("click", function () {
      Ct.toggleClass("customizer-open");
    }),
      Ct.on("click", ".customizer-colors__item", function () {
        i && o.removeChild(i);
        var n = t(this).attr("data-custom-color");
        e(n), o.appendChild(i);
      }),
      Ct.on("click", ".customizer-menu__item", function () {
        Y.hasClass("menu-mobile-left") &&
        "right" === this.textContent.toLocaleLowerCase()
          ? (Y.removeClass("menu-mobile-left"), Y.addClass("menu-mobile-right"))
          : Y.hasClass("menu-mobile-right") &&
            "left" === this.textContent.toLocaleLowerCase() &&
            (Y.addClass("menu-mobile-left"),
            Y.removeClass("menu-mobile-right"));
      }),
      document
        .querySelector(".customizer-default")
        .addEventListener("click", function () {
          document.location.reload();
        }),
      sessionStorage &&
        !sessionStorage.getItem("customizerAlert") &&
        setTimeout(function () {
          var t = Ct.find(".customizer-alert");
          t.css("display", "block").smoothAnimate({ opacity: [0, 1] }),
            Ct.find(".customizer-alert__btn").one("click", function () {
              sessionStorage.setItem("customizerAlert", "1"),
                t.smoothAnimate(
                  { opacity: [1, 0] },
                  {
                    complete: function (t) {
                      t[0].style.display = "none";
                    },
                  }
                );
            });
        }, 15e3);
  }
  function z(t, e) {
    function i() {
      s || ((s = !0), e && e());
    }
    var n = document.createElement("script");
    n.src = t;
    var o = document.getElementsByTagName("script")[0];
    o.parentNode.insertBefore(n, o);
    var s = !1;
    (n.onload = i),
      (n.onreadystatechange = function () {
        ("loaded" !== this.readyState && "complete" !== this.readyState) ||
          setTimeout(i, 0);
      });
  }
  function D() {
    var e = document.querySelectorAll(".video-bg__container");
    if (e.length)
      for (var i = 0; i < e.length; i++) {
        var n = e[i],
          o = t(n).parents(".section")[0];
        n.style.position = "absolute";
        var s = o.getBoundingClientRect(),
          a = s.height,
          r = s.width,
          l = 1.7777777777777777;
        if (L < Q) {
          var c = r / l,
            u = a * l;
          a > c
            ? ((n.style.width = u + "px"),
              (n.style.height = a + "px"),
              (n.style.top = "0px"),
              (n.style.left = (r - u) / 2 + "px"))
            : ((n.style.width = r + "px"),
              (n.style.height = c + "px"),
              (n.style.top = (a - c) / 2 + "px"),
              (n.style.left = "0px"));
        } else {
          var c = r / l,
            u = a * l;
          r > u
            ? ((n.style.width = r + "px"),
              (n.style.height = c + "px"),
              (n.style.top = (a - c) / 2 + "px"),
              (n.style.left = "0px"))
            : ((n.style.width = u + "px"),
              (n.style.height = a + "px"),
              (n.style.top = "0px"),
              (n.style.left = (r - u) / 2 + "px"));
        }
      }
  }
  function R() {
    var e = t(".tabs");
    e.length &&
      e.each(function (e, i) {
        var n = t(i).find(".tabs__link"),
          o = t(i).find(".tabs__link_active"),
          s = t(i).siblings(".tabs__content"),
          a = o[0].getAttribute("href");
        t(a).show(400, function () {
          t(window).trigger("resize");
        }),
          n.on("click", function (e) {
            if ((e.preventDefault(), !t(this).hasClass("tabs__link_active"))) {
              var i = this.getAttribute("href");
              n.removeClass("tabs__link_active"),
                t(this).addClass("tabs__link_active"),
                s.find(".tabs__tab").not(i).css("display", "none"),
                t(i).fadeIn(400, function () {
                  window.dispatchEvent(new Event("resize"));
                });
            }
          });
      });
  }
  var H,
    L,
    Q,
    U,
    $,
    W,
    V,
    J,
    Y = t("body"),
    X = t(".header"),
    K = document.documentElement || document.body,
    G = document.querySelectorAll(".equal-cols"),
    Z = t(".portfolio-masonry"),
    tt = t(".s-blog-masonry"),
    et = document.getElementById("wpadminbar");
  !(function () {
    for (
      var t = 0, e = ["ms", "moz", "webkit", "o"], i = 0;
      i < e.length && !window.requestAnimationFrame;
      ++i
    )
      (window.requestAnimationFrame = window[e[i] + "RequestAnimationFrame"]),
        (window.cancelAnimationFrame =
          window[e[i] + "CancelAnimationFrame"] ||
          window[e[i] + "CancelRequestAnimationFrame"]);
    window.requestAnimationFrame ||
      (window.requestAnimationFrame = function (e, i) {
        var n = new Date().getTime(),
          o = Math.max(0, 16 - (n - t)),
          s = window.setTimeout(function () {
            e(n + o);
          }, o);
        return (t = n + o), s;
      }),
      window.cancelAnimationFrame ||
        (window.cancelAnimationFrame = function (t) {
          clearTimeout(t);
        });
  })(),
    (function () {
      if (
        ("performance" in window == 0 && (window.performance = {}),
        (Date.now =
          Date.now ||
          function () {
            return new Date().getTime();
          }),
        "now" in window.performance == 0)
      ) {
        var t = Date.now();
        performance.timing &&
          performance.timing.navigationStart &&
          (t = performance.timing.navigationStart),
          (window.performance.now = function () {
            return Date.now() - t;
          });
      }
    })(),
    (t.browser = {
      webkit: /WebKit/i.test(navigator.userAgent),
      mobile: /Android|iPhone|iPad|iPod|IEMobile|BlackBerry|Opera Mini/i.test(
        navigator.userAgent
      ),
      ie: /Trident/i.test(navigator.userAgent),
      safari: /Safari/i.test(navigator.userAgent),
      loc: location.host,
    }),
    (t.platform = {
      mac: navigator.platform.toLowerCase().indexOf("mac") !== -1,
      android: /(android)/i.test(navigator.userAgent),
      ios: /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream,
    }),
    (t.supp = {
      cssTransform3d: null,
      cssTransform2d: null,
      cssFilter: null,
      transitionEnd: null,
      transition: null,
      animationEnd: null,
      animation: null,
      init: function () {
        (this.el = document.getElementsByTagName("body")[0]),
          this.cssFilterCheck(),
          this.css3dTransformCheck(),
          this.transitionCheck(),
          this.animationCheck();
      },
      cssFilterCheck: function () {
        var t = { webkitFilter: "-webkit-filter", filter: "filter" };
        for (var e in t) {
          this.el.style[e] = "grayscale(0)";
          var i = getComputedStyle(this.el)[e];
          void 0 !== i &&
            null !== i &&
            "none" !== i &&
            i.length &&
            (this.cssFilter = e),
            this.el.style.removeProperty(t[e]);
        }
      },
      css3dTransformCheck: function () {
        var t = {
          webkitTransform: "-webkit-transform",
          msTransform: "-ms-transform",
          transform: "transform",
        };
        for (var e in t) {
          this.el.style[e] = "translate3d(1px, 1px, 1px)";
          var i = getComputedStyle(this.el).getPropertyValue(t[e]);
          if (void 0 !== i && null !== i && "none" !== i && i.length)
            this.cssTransform3d = e;
          else {
            this.el.style[e] = "translate(1px, 1px)";
            var i = getComputedStyle(this.el).getPropertyValue(t[e]);
            void 0 !== i &&
              null !== i &&
              "none" !== i &&
              i.length &&
              (this.cssTransform2d = e);
          }
          this.el.style.removeProperty(t[e]);
        }
      },
      transitionCheck: function () {
        var t = {
          WebkitTransition: "webkitTransitionEnd",
          transition: "transitionend",
        };
        for (var e in t)
          void 0 !== this.el.style[e] &&
            ((this.transitionEnd = t[e]), (this.transition = e));
      },
      animationCheck: function () {
        var t = {
          WebkitAnimation: "webkitAnimationEnd",
          animation: "animationend",
        };
        for (var e in t)
          void 0 !== this.el.style[e] &&
            ((this.animationEnd = t[e]), (this.animation = e));
      },
    }),
    t.supp.init(),
    i(),
    n(),
    o(),
    a(),
    r(),
    u(),
    d(),
    h(),
    (t.fn.slideLikeAPro = function (e) {
      var i = {
          itemClass: "pro-item",
          itemsContainerClass: "pro-items",
          autoplay: !0,
          autoplayTimeout: 3e3,
          firstRunDelay: 0,
          timeline: !0,
          loop: !0,
          nav: !0,
          navClass: ["pro-controls-prev ico-111", "pro-controls-next ico-112"],
          navContainerClass: "pro-controls",
        },
        n = JSON.parse(this[0].getAttribute("data-slide-like-a-pro")),
        o = this,
        s = o.children(),
        a = s.length,
        r = t.extend({}, i, e, n),
        l = {
          start: null,
          prevItem: null,
          nextItem: 1,
          currentItem: 0,
          currentTimeline: null,
          progress: 0,
          offsetTop: null,
          height: null,
          init: function () {
            this.onResize(),
              this.prepare(),
              this.goToSlide(0),
              this.loop(),
              this.setHandlers();
          },
          prepare: function () {
            if (
              (s.wrapAll('<div class="' + r.itemsContainerClass + '"></div>'),
              s.addClass(r.itemClass),
              "true" === r.timeline &&
                s.append('<div class="' + r.itemClass + '__timeline"></div>'),
              "true" === r.nav)
            ) {
              var e = t(
                  '<div class="' +
                    r.navClass[0] +
                    '"></div><div class="' +
                    r.navClass[1] +
                    '"></div>'
                ),
                i = t('<div class="' + r.navContainerClass + '"></div>');
              i.append(e), o.append(i);
            }
            t(s[0]).addClass(r.itemClass + "_show");
          },
          goToSlide: function (e) {
            if (((this.start = performance.now()), null !== this.prevItem)) {
              var i = getComputedStyle(s[this.prevItem])[
                  t.supp.transition + "Duration"
                ],
                n = getComputedStyle(s[this.prevItem])[
                  t.supp.transition + "Delay"
                ];
              (i =
                i.toLowerCase().indexOf("ms") > -1
                  ? parseInt(i, 10)
                  : 1e3 * parseFloat(i)),
                (n =
                  n.toLowerCase().indexOf("ms") > -1
                    ? parseInt(n, 10)
                    : 1e3 * parseFloat(n)),
                (this.slideTransDur = i + n),
                (this.activeClassAdded = !1),
                t(s[this.prevItem]).removeClass(r.itemClass + "_active");
            } else
              setTimeout(function () {
                t(s[0])
                  .addClass(r.itemClass + "_active")
                  .removeClass(r.itemClass + "_show");
              }, r.firstRunDelay);
            (this.currentItem = e),
              "true" === r.timeline &&
                (this.currentTimeline = s[this.currentItem].querySelector(
                  "." + r.itemClass + "__timeline"
                ));
          },
          prevSlide: function () {
            (this.nextItem = this.currentItem),
              (this.prevItem = this.currentItem),
              this.goToSlide(
                --this.currentItem >= 0 ? this.currentItem-- : a - 1
              );
          },
          nextSlide: function () {
            (this.prevItem = this.currentItem),
              (this.nextItem = ++this.currentItem < a ? this.currentItem++ : 0),
              this.goToSlide(this.nextItem);
          },
          loop: function (e) {
            var i = this,
              n = w(this.offsetTop, this.height);
            if (n) {
              if ("false" === r.loop && this.currentItem + 1 === a) return;
              var o = e - this.start;
              o > r.autoplayTimeout && (o = r.autoplayTimeout),
                o >= this.slideTransDur &&
                  !this.activeClassAdded &&
                  (t(s[i.currentItem]).addClass(r.itemClass + "_active"),
                  (this.activeClassAdded = !0)),
                (this.progress = (100 * o) / r.autoplayTimeout),
                "true" === r.autoplay &&
                  ("true" === r.timeline && this.timeline(),
                  o >= r.autoplayTimeout && this.nextSlide());
            }
            requestAnimationFrame(function (t) {
              i.loop(t);
            });
          },
          timeline: function () {
            this.currentTimeline.style.width = this.progress + "%";
          },
          setHandlers: function () {
            var e = this;
            if ("true" === r.nav) {
              var i = t("." + r.navClass[0].replace(" ", ".")),
                n = t("." + r.navClass[1].replace(" ", "."));
              o
                .mouseenter(function () {
                  i.add(n).addClass("animated");
                })
                .mouseleave(function () {
                  i.add(n).removeClass("animated");
                }),
                n.on("click", function () {
                  e.nextSlide();
                }),
                i.on("click", function () {
                  e.prevSlide();
                });
            }
            t(window).smartresize(this.onResize);
          },
          onResize: function () {
            var t = o.parents(".section");
            o[0].style.height = "";
            var e = t.height();
            o[0].style.height = e + "px";
            var i = o[0].getBoundingClientRect();
            (this.offsetTop = i?.top + H), (this.height = i.bottom - i?.top);
          },
        };
      return l.init(), this;
    }),
    window.addEventListener("scroll", v, !1),
    (function (t, e) {
      var i = function (t, e, i) {
        var n;
        return function () {
          function o() {
            i || t.apply(s, a), (n = null);
          }
          var s = this,
            a = arguments;
          n ? clearTimeout(n) : i && t.apply(s, a),
            (n = setTimeout(o, e || 100));
        };
      };
      t.fn[e] = function (t) {
        return t ? this.bind("resize", i(t)) : this.trigger(e);
      };
    })(t, "smartresize"),
    t(window).smartresize(function () {
      b(),
        (Z.length || tt.length) &&
          Z.add(tt).one("layoutComplete", function () {
            b();
          });
    });
  var it = {
      anchors: document.querySelectorAll(
        ".section, footer, #tab-title-reviews"
      ),
      anchorButtons: document.getElementsByClassName("scroll-to"),
      section: [],
      currentSection: null,
      menuItems: document.getElementsByClassName("menu__link"),
      position: H,
      anchor: document.querySelectorAll(".s-portfolio"),
      headerModified: !1,
      init: function () {
        this.makeScrolling(),
          !r() && document.querySelector(".header")
            ? ((this.header = document.querySelector(".header")),
              (this.headerHeight = d()
                ? document.querySelector(".header").offsetHeight
                : 0),
              this.updateOffsets(),
              this.headerObserve())
            : (this.updateOffsets(), (this.headerHeight = 0)),
          $ && this.makeMenuObserve(),
          t.browser.mobile ||
            t.platform.mac ||
            !Y.hasClass("smooth-scroll") ||
            this.custom();
      },
      scrollTo: function (e) {
        if ("number" == typeof e) this.position = e;
        else if (t(e).length) {
          var i = document.getElementById(e.substr(1)),
            n = i.getBoundingClientRect();
          this.position = Math.floor(n?.top) + H - this.headerHeight + 1;
        }
        this.position !== H &&
          Y.smoothAnimate(
            { scrollTop: this.position },
            {
              duration: 800,
              easing: "easeOutSine",
              queue: !1,
              step: function (t, e, i) {
                this.position = i;
              }.bind(this),
              complete: function () {
                this.dontChangeMenu = !1;
              }.bind(this),
            }
          );
      },
      custom_: function () {
        var e = 3,
          i = 0,
          n = 0,
          o = 16;
        t(window).on(
          "mousewheel DOMMouseScroll",
          function (t) {
            t.preventDefault(), (n = Date.now());
            var s = n - o,
              a = t.originalEvent,
              r = a.wheelDelta < 0 || a.detail > 0 ? 1 : -1;
            (i += 50 * r * e),
              (this.position += i),
              s > 16 && this.scrollTo(this.position),
              (o = n);
          }.bind(this)
        );
      },
      custom: function () {
        function t() {
          o < -i || o > i
            ? ((this.position += o),
              window.scrollTo(0, Math.round(this.position)),
              (o *= e))
            : (o = 0),
            requestAnimationFrame(function () {
              t.call(it);
            });
        }
        var e = 0.95,
          i = 0.1,
          n = 4,
          o = 0;
        t.call(this),
          window.addEventListener(
            "wheel",
            function (t) {
              t.preventDefault();
              var e = t.deltaY > 0 ? 1 : -1;
              (this.position = H), (o += n * e);
            }.bind(this),
            { passive: !1 },
            !1
          );
      },
      test: function () {
        var t,
          e = performance.now(),
          i = 0,
          n = [];
        Y.smoothAnimate(
          { scrollTop: U - Q },
          {
            duration: 12e3,
            easing: "linear",
            step: function (o, s, a, r) {
              (this.position = Math.ceil(a)),
                (t = r || performance.now()),
                (i = Math.round(1e3 / (t - e))),
                (e = t),
                n.push(i);
            }.bind(this),
            complete: function () {
              this.dontChangeMenu = !1;
              var t = 0;
              n.forEach(function (e) {
                t += e;
              }),
                console.log("%cFPS: " + t / n.length, "color:blue");
            }.bind(this),
          }
        );
      },
      prepareAnchors: function () {
        for (
          var e = '<div class="section-arrow ico-110"></div>', i = 0;
          i < this.anchor.length;
          i++
        ) {
          var n = this.anchor[i];
          this.anchorButtons.push(e), t(e).prependTo(n);
        }
      },
      makeScrolling: function () {
        var e = this;
        t(this.anchorButtons).on("click", function (t) {
          t.preventDefault();
          var i = this.getAttribute("href");
          i && i.match(/#.*/)
            ? e.scrollTo(i.match(/#.*/)[0])
            : e.currentSection < e.anchor.length
            ? e.scrollTo(e.section[e.currentSection + 1].hash)
            : e.scrollTo(e.section[0].hash);
        });
      },
      testForMenuChange: function () {
        if (!this.dontChangeMenu)
          for (var t = 0; t < this.section.length; t++)
            H + Q - this.headerHeight >=
            this.section[this.section.length - 1]?.top +
              this.section[this.section.length - 1].height
              ? this.setMenuActiveItem(this.section.length - 1)
              : H >= this.section[t]?.top &&
                H < this.section[t]?.top + this.section[t].height &&
                this.setMenuActiveItem(t);
      },
      makeMenuObserve: function () {
        var e = this;
        t(e.menuItems).on("click", function (i) {
          var n = this.getAttribute("href");
          t(n).length && i.preventDefault();
          for (var o = 0; o < e.section.length; o++)
            if (n === e.section[o].hash) {
              e.scrollTo(n), e.setMenuActiveItem(o, !0);
              var s = e.section[o].hash;
              window.location.hash = "!" + s.substr(1);
              break;
            }
          c() &&
            (Y.removeClass("menu-mobile-opened"),
            setTimeout(function () {
              t(".menu-mobile-social").remove();
            }, 400));
        });
      },
      headerSticky: {
        on: function () {
          t(this.header).addClass("header-sticky header-dark"),
            (this.headerModified = !0),
            V &&
              !c() &&
              (this.header.nextElementSibling.style.marginTop =
                this.headerHeight + "px");
        },
        off: function () {
          c() ||
            ((this.header.style.top = "-" + this.headerHeight + "px"),
            V
              ? (t(this.header).removeClass("header-sticky"),
                this.header.removeAttribute("style"),
                (this.header.nextElementSibling.style.marginTop = ""))
              : setTimeout(
                  function () {
                    t(this.header).removeClass("header-sticky header-dark"),
                      this.header.removeAttribute("style");
                  }.bind(this),
                  400
                )),
            (this.headerModified = !1);
        },
      },
      headerObserve: function () {
        d() && H >= this.offsetToChangeHeader && !this.headerModified
          ? this.headerSticky.on.call(this)
          : H < this.offsetToChangeHeader &&
            this.headerModified &&
            this.headerSticky.off.call(this);
      },
      setMenuActiveItem: function (t, e) {
        if (this.currentSection !== t && !this.dontChangeMenu) {
          e && (this.dontChangeMenu = !0), (this.currentSection = t);
          for (var i = 0; i < this.menuItems.length; i++) {
            var n = this.menuItems[i];
            this.section[t].hash === n.getAttribute("href")
              ? n.classList.add("menu__link_active")
              : n.classList.remove("menu__link_active");
          }
        }
      },
      updateOffsets: function () {
        this.section = [];
        for (var t = 0; t < this.anchors.length; t++) {
          var e = this.anchors[t].getBoundingClientRect();
          this.section[t] = {
            hash: "#" + this.anchors[t].id,
            top:
              0 === t
                ? Math.floor(e?.top) + H
                : Math.floor(e?.top) + H - this.headerHeight,
            height: Math.floor(e.bottom - e?.top),
          };
        }
        this.offsetToChangeHeader =
          0 === this.section[0]?.top
            ? this.section[0].height - (V ? 0 : this.headerHeight)
            : 0;
      },
      scrollToHash: function () {
        if (window.location.hash) {
          var e = window.location.hash.replace(/!/, "");
          this.scrollTo(e),
            t(window).load(
              function () {
                this.scrollTo(e);
              }.bind(this)
            );
        }
      },
    },
    nt = {
      animateItems: document.getElementsByClassName("fx"),
      offsets: [],
      animated: [],
      animations: {},
      properties: {},
      complete: 0,
      init: function () {
        for (var t = 0; t < this.animateItems.length; t++) {
          var e = this.animateItems[t];
          (this.animated[t] = !1),
            (this.animations[t] = {
              "animation-name": e.getAttribute("data-animation-name"),
              "animation-duration": e.getAttribute("data-animation-duration"),
              "animation-delay": e.getAttribute("data-animation-delay"),
              "animation-fill-mode": e.getAttribute("data-animation-fill-mode"),
              "animation-iteration-count": e.getAttribute(
                "data-animation-iteration-count"
              ),
              "animation-visible-percent":
                e.getAttribute("data-animation-visible-percent") || 10,
            });
        }
        this.updateOffsets(), this.setHandlers();
      },
      makeAnimate: function () {
        for (var e = 0; e < this.animateItems.length; e++)
          if (this.animations[e] && !this.animated[e]) {
            var i = this.animateItems[e],
              n = w(this.offsets[e]?.top, this.offsets[e].height);
            if (n >= this.animations[e]["animation-visible-percent"]) {
              var o = this.animations[e]["animation-name"],
                s = this.animations[e]["animation-duration"],
                a = this.animations[e]["animation-delay"],
                r = this.animations[e]["animation-fill-mode"],
                l = this.animations[e]["animation-iteration-count"];
              o
                ? (this.properties["animation-name"] = o)
                : delete this.properties["animation-name"],
                s
                  ? (this.properties["animation-duration"] = s)
                  : delete this.properties["animation-duration"],
                a
                  ? (this.properties["animation-delay"] = a)
                  : delete this.properties["animation-delay"],
                r
                  ? (this.properties["animation-fill-mode"] = r)
                  : delete this.properties["animation-fill-mode"],
                l
                  ? (this.properties["animation-iteration-count"] = l)
                  : delete this.properties["animation-iteration-count"],
                t(i)
                  .addClass("animated")
                  .css(this.properties)
                  .trigger("animationOnScrollStarted"),
                (this.animated[e] = !0),
                this.complete++;
            }
          } else if (this.complete === this.animateItems.length) {
            this.destroy();
            break;
          }
      },
      setHandlers: function () {
        t(window).on("scroll.animateOnScroll", function () {
          requestAnimationFrame(function () {
            nt && st.complete && nt.makeAnimate();
          });
        }),
          t(this.animateItems).on(
            t.supp.animationEnd + " " + t.supp.transitionEnd,
            function () {
              t(this).trigger("animationOnScrollEnded");
            }
          );
      },
      updateOffsets: function () {
        this.offsets = [];
        for (var t = 0; t < this.animateItems.length; t++) {
          var e = this.animateItems[t].getBoundingClientRect();
          this.offsets[t] = {
            top: Math.floor(e?.top) + H,
            height: Math.floor(e.bottom - e?.top),
          };
        }
      },
      destroy: function () {
        t(window).off(".animateOnScroll"),
          t(this.animateItems).off(t.supp.animationEnd);
        for (var e in this) delete this[e];
        nt = null;
      },
    },
    ot = {
      parallaxItems: document.getElementsByClassName("parallax"),
      options: [],
      offsetsElement: [],
      offsetsParent: [],
      init: function () {
        this.settings();
      },
      settings: function () {
        for (var e, i, n, o, s, a = 0; a < this.parallaxItems.length; a++) {
          var r = this.parallaxItems[a],
            l = r.parentNode;
          t(l).addClass("parallax-wrapper");
          var c = JSON.parse(r.getAttribute("data-parallax"));
          (e = c?.speed || 0.5),
            (e = e >= 0 && e <= 1 ? e : 0.5),
            (i = c?.fading || "true"),
            (n = c.min - i || 70),
            (o = c?.scale || !1),
            (s = c.max - o || 1.5),
            (this.options[a] = {
              speed: e,
              fading: i,
              minFading: n,
              scale: o,
              maxScale: s,
            });
        }
      },
      makeParallax: function () {
        for (
          var e = et ? et.offsetHeight : 0, i = 0;
          i < this.parallaxItems.length;
          i++
        ) {
          var n = this.parallaxItems[i],
            o = w(this.offsetsParent[i]?.top, this.offsetsParent[i].height);
          if (o) {
            if ("true" === this.options[i]?.fading) {
              var a = s(
                this.offsetsParent[i].height <= Q
                  ? (this.options[i].minFading * o) / 100
                  : this.options[i].minFading
              );
              if (t.supp.cssFilter)
                n.style[t.supp.cssFilter] = "brightness(" + a + "%)";
              else {
                var r = document.getElementById("cssFilterFallback" + i);
                null === r &&
                  ((r = document.createElement("div")),
                  (r.id = "cssFilterFallback" + i),
                  (r.style.position = "absolute"),
                  (r.style.width = "100%"),
                  (r.style.height = "100%"),
                  (r.style.top = "0"),
                  (r.style.left = "0"),
                  (r.style.backgroundColor = "#000"),
                  n.insertBefore(r, n.firstChild)),
                  (r.style.opacity = 1 - a / 100);
              }
            }
            if (
              this.offsetsParent[i].height >= this.offsetsElement[i].height &&
              (this.stopParallax(n), this.offsetsParent[i]?.top !== e)
            )
              continue;
            var l =
                "true" === this.options[i]?.scale
                  ? 1 + ((this.options[i].maxScale - 1) * (100 - o)) / 100
                  : 1,
              c =
                this.offsetsElement[i].height - this.offsetsParent[i].height > Q
                  ? Q
                  : this.offsetsElement[i].height -
                    this.offsetsParent[i].height,
              u =
                this.offsetsParent[i]?.top > e
                  ? this.offsetsParent[i].height
                  : 0,
              c = 0 === u && c < 0 ? -c : c,
              d = s(
                ((((100 * (this.offsetsParent[i]?.top - H)) / (Q + u)) * c) /
                  100) *
                  this.options[i]?.speed
              );
            t.supp.cssTransform3d
              ? (n.style[t.supp.cssTransform3d] =
                  "translate3d(0," +
                  -d +
                  "px,0) scale3d(" +
                  l +
                  ", " +
                  l +
                  ", 1)")
              : (n.style[t.supp.cssTransform2d] =
                  "translate(0," + -d + "px) scale(" + l + ")");
          }
        }
      },
      updateOffsets: function () {
        (this.offsetsElement = []), (this.offsetsParent = []);
        for (var t = 0; t < this.parallaxItems.length; t++) {
          var e = this.parallaxItems[t],
            i = e.getBoundingClientRect(),
            n = e.parentNode,
            o = n.getBoundingClientRect();
          (this.offsetsElement[t] = {
            top: Math.floor(i?.top) + H,
            height: _(e).height || Math.floor(i.bottom - i?.top),
          }),
            (this.offsetsParent[t] = {
              top: Math.floor(o?.top) + H,
              height: Math.floor(o.bottom - o?.top),
            }),
            this.offsetsElement[t].height > this.offsetsParent[t].height &&
              0 !== this.offsetsParent[t]?.top &&
              (e.style.height = this.offsetsElement[t].height + "px");
        }
      },
      stopParallax: function (e) {
        e.style.removeProperty("height"),
          e.style.removeProperty(
            t.supp.cssTransform3d
              ? t.supp.cssTransform3d
              : t.supp.cssTransform2d
          );
      },
    };
  ot.init();
  var st = {
    imgs: document.querySelectorAll(
      ".main-slider__item, .main-static__bg, .parallax"
    ),
    videos: document.querySelectorAll(".video-bg__video"),
    currentItem: 0,
    totalItems: function () {
      return this.imgs.length;
    },
    preloaderPage: document.querySelector(".p-preloader"),
    progressBar: document.querySelector(".p-preloader__progressbar"),
    progressBarPercentage: document.querySelector(".p-preloader__percentage"),
    progress: 0,
    done: !1,
    complete: !1,
    init: function () {
      this._loadResources();
    },
    _pagePreload: function () {
      return (
        this.totalItems()
          ? (this.progress = (100 * this.currentItem) / this.totalItems())
          : (this.progress = 100),
        this.progress > 100 && (this.progress = 100),
        this.preloaderPage
          ? void t(this.progressBar)
              .stop()
              .animate(
                { width: this.progress + "%" },
                {
                  duration: 400,
                  step: function (t) {
                    this.progressBarPercentage.textContent = Math.round(t);
                  }.bind(this),
                  complete: function () {
                    this.complete ||
                      (t(this.progressBar).fadeOut(
                        200,
                        function () {
                          this._done();
                        }.bind(this)
                      ),
                      t(this.progressBarPercentage).fadeOut(200));
                  }.bind(this),
                }
              )
          : void (this.complete || this._done())
      );
    },
    _loadResources: function () {
      if (!this.totalItems()) return void this._pagePreload();
      for (var t = 0; t < this.imgs.length; t++)
        f(
          this.imgs[t],
          function () {
            this.currentItem++, this._pagePreload();
          }.bind(this)
        );
    },
    _done: function () {
      console && console.log("done"),
        n(),
        this.done ||
          ((this.done = !0),
          $ || W ? p() : g(),
          ot.updateOffsets(),
          ot.makeParallax(),
          it.init(),
          nt.init(),
          m(),
          t(window).resize(),
          e(
            800,
            function () {
              (this.complete = !0),
                this.preloaderPage &&
                  (this.preloaderPage.style.display = "none"),
                nt && nt.makeAnimate(),
                $ && (it.testForMenuChange(), it.scrollToHash());
            }.bind(this)
          ));
    },
  };
  st.init();
  var at = t(".menu__link, .submenu__link").not(":only-child"),
    rt = t(".menu__item .submenu__link").not(":only-child"),
    lt = t(".menu"),
    ct = t(".footer-social"),
    ut = t(".footer"),
    dt = t(".wrapper-content");
  t(".menu-toggle").on("click", function () {
    Y.hasClass("menu-mobile-opened")
      ? (Y.removeClass("menu-mobile-opened"),
        !r() &&
          ct.children().length &&
          setTimeout(function () {
            t(".menu-mobile-social").remove();
          }, 400))
      : (!r() &&
          ct.children().length &&
          lt.append(
            '<li class="menu__item menu-mobile-social">' + ct.html() + "</li>"
          ),
        Y.addClass("menu-mobile-opened"));
  }),
    r()
      ? (ct.children().length &&
          lt.append(
            '<li class="menu__item menu-mobile-social">' + ct.html() + "</li>"
          ),
        c()
          ? (Y.removeClass("menu-mobile-opened"),
            dt.css("margin-bottom", "auto"),
            ut.css({ position: "relative", width: "auto" }))
          : l
          ? (dt.css("margin-bottom", ut.height() + "px"),
            ut.css({ position: "fixed", width: "100%" }))
          : (Y.addClass("menu-mobile-opened"),
            dt.css("margin-bottom", ut.height() + "px"),
            ut.css({
              position: "fixed",
              width: "calc(100% - " + lt.width() + "px)",
            })))
      : dt.css("margin-bottom", ut.height() + "px"),
    C(),
    at.on("click", function (e) {
      (c() || r()) && t(this).toggleClass("menu__link_pressed");
    }),
    t(".submenu")
      .parent(".menu__item, .submenu__item")
      .on("click", function () {
        (c() || r()) && t(this).children(".submenu").slideToggle();
      }),
    t(".submenu__item").on("click", function (t) {
      t.stopPropagation();
    }),
    x(),
    A(),
    (function () {
      var e = document.getElementsByClassName("accordion__title"),
        i = document.getElementsByClassName("accordion__content");
      t(e).on("click", function () {
        var n = t(this).next().hasClass("opened");
        t(e).removeClass("opened"),
          t(i).removeClass("opened").slideUp(),
          n ||
            (t(this).addClass("opened"),
            t(this).next().slideDown().addClass("opened"));
      });
    })();
  var ht = {
    initAjaxSubmit: function (t) {
      var t = jQuery.extend(
          {
            selector: "form[data-fw-form-id]",
            ajaxUrl: lptAjax.ajaxurl,
            loading: function (t, e) {
              t.$form.css("position", "relative"),
                t.$form.find("> .fw-form-loading").remove(),
                e &&
                  t.$form.append(
                    '<div class="fw-form-loading" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: rgba(255,255,255,0.1);"></div>'
                  );
            },
            afterSubmitDelay: function (t) {},
            onErrors: function (t, i) {
              console.log("onErrors"),
                e
                  ? ht.backend.showFlashMessages(
                      ht.backend.renderFlashMessages({ error: i.errors })
                    )
                  : jQuery.each(i.errors, function (t, e) {
                      E(e, "", !0);
                    });
            },
            hideErrors: function (t) {
              t.$form.find(".form-error").remove();
            },
            onAjaxError: function (t, e) {
              console.error(e.jqXHR, e.textStatus, e.errorThrown),
                E("<span>Ajax error (more details in console)</span>", "", !0);
            },
            onSuccess: function (t, i) {
              if (e)
                ht.backend.showFlashMessages(
                  ht.backend.renderFlashMessages(i.flash_messages)
                );
              else {
                var n = "";
                jQuery.each(i.flash_messages, function (t, e) {
                  switch (t) {
                    case "success":
                      n = "theme-color";
                      break;
                    default:
                      n = "";
                  }
                  jQuery.each(e, function (t, e) {
                    E('<span class="' + n + '">' + e + "</span>", "", !0);
                  });
                }),
                  setTimeout(function () {
                    O();
                  }, 3e3),
                  t.$form.on("submit", function (t) {
                    t.preventDefault(), t.stopPropagation();
                  });
              }
            },
          },
          t || {}
        ),
        e =
          "undefined" != typeof adminpage &&
          jQuery(document.body).hasClass("wp-admin"),
        i = !1;
      jQuery(document.body).on("submit", t.selector, function (e) {
        if ((e.preventDefault(), i))
          return void console.warn("Working... Try again later.");
        var n = jQuery(this);
        if (!n.is("form[data-fw-form-id]"))
          return void console.error(
            "This is not a FW_Form",
            "Selector:".opts.selector,
            "Form:",
            n
          );
        var o = n.find(":submit:focus");
        o.length || (o = n.find("[clicked]:submit")),
          n.find("[clicked]:submit").removeAttr("clicked");
        var s = { $form: n, $submitButton: o };
        t.hideErrors(s);
        var a = parseInt(t.loading(s, !n.hasClass("fw-silent-submit")));
        (a = isNaN(a) || a < 0 ? 0 : a),
          n.removeClass("fw-silent-submit"),
          (i = !0),
          setTimeout(function () {
            a && t.afterSubmitDelay(s),
              jQuery
                .ajax({
                  type: "POST",
                  url: t.ajaxUrl,
                  data:
                    n.serialize() +
                    (o.length
                      ? "&" + o.attr("name") + "=" + o.attr("value")
                      : ""),
                  dataType: "json",
                })
                .done(function (e) {
                  (i = !1),
                    t.loading(s, !1),
                    e.success
                      ? (t.onSuccess(s, e.data), n[0].reset())
                      : t.onErrors(s, e.data);
                })
                .fail(function (e, n, o) {
                  (i = !1),
                    t.loading(s, !1),
                    t.onAjaxError(s, {
                      jqXHR: e,
                      textStatus: n,
                      errorThrown: o,
                    });
                });
          }, a);
      });
    },
    backend: {
      showFlashMessages: function (t) {
        for (
          var e = jQuery(".wrap h2:first");
          e
            .next()
            .is(
              ".fw-flash-messages, .fw-flash-message, .updated, .update-nag, .error"
            );

        )
          e.next().remove();
        e.after('<div class="fw-flash-messages">' + t + "</div>"),
          jQuery(document.body).animate({ scrollTop: 0 }, 300);
      },
      renderFlashMessages: function (t) {
        var e = [],
          i = [],
          n = "";
        return (
          jQuery.each(t, function (t, o) {
            switch (((i = []), t)) {
              case "error":
                n = "error";
                break;
              case "warning":
                n = "update-nag";
                break;
              default:
                n = "updated";
            }
            jQuery.each(o, function (t, e) {
              i.push(
                '<div class="' + n + ' fw-flash-message"><p>' + e + "</p></div>"
              );
            }),
              i.length &&
                e.push(
                  '<div class="fw-flash-type-' +
                    t +
                    '">' +
                    i.join('</div><div class="fw-flash-type-' + t + '">') +
                    "</div>"
                );
          }),
          e.join("")
        );
      },
    },
    frontend: {
      renderFlashMessages: function (t) {
        var e = [],
          i = [];
        return (
          jQuery.each(t, function (t, n) {
            (i = []),
              jQuery.each(n, function (t, e) {
                i.push('<li class="fw-flash-message">' + e + "</li>");
              }),
              i.length &&
                e.push(
                  '<ul class="fw-flash-type-' +
                    t +
                    '">' +
                    i.join('</ul><ul class="fw-flash-type-' + t + '">') +
                    "</ul>"
                );
          }),
          e.join("")
        );
      },
    },
  };
  ht.initAjaxSubmit({
    selector: 'form[data-fw-form-id][data-fw-ext-forms-type="contact-forms"]',
    ajaxUrl: lptAjax.ajaxurl,
  }),
    t(".footer-form").submit(function (e) {
      var i = t(this),
        n = i.find('[type="submit"]'),
        o = n.data("responses");
      S(!0, 400, 0, function () {
        t.ajax({
          type: "POST",
          data:
            i.serialize() + "&" + t.param({ action: "nowadays_contact_form" }),
          url: lptAjax.ajaxurl,
          success: function (t) {
            t
              ? (i[0].reset(),
                E('<span class="theme-color">' + o.success + "</span>", "", !1),
                setTimeout(function () {
                  O();
                }, 3e3))
              : E(o.failure, "", !1);
          },
          error: function () {
            E(o.failure, "", !1);
          },
        });
      }),
        e.preventDefault();
    }),
    I();
  var mt = t(".modal"),
    ft = t(".modal__content"),
    pt = t(".overlay"),
    gt = t(".preloader"),
    vt = t(".modal__info"),
    yt = 400;
  pt.add(t(".modal__close")).on("click", O);
  var bt,
    wt,
    _t = t(".preview-photo, .preview-video, .preview-custom");
  ($ || W) && q(_t),
    (function (e, i, n, o) {
      function s(t) {
        return t
          .toString()
          .match(/^(\+\=|\-\=|\*\=|\/\=)?([+-]?(?:\d+|\d*\.\d+))([a-z]*|%)$/i);
      }
      function a(t) {
        return (
          (t = r(t)), /(^(zIndex|fontWeight|opacity)$)/.test(t) ? "" : "px"
        );
      }
      function r(t) {
        return t.replace(/-(\w)/g, function (t, e) {
          return e.toUpperCase();
        });
      }
      function l(e, i, n) {
        if (
          (Array.isArray(e) &&
            ((i = e),
            (e = n
              ? n
              : u + "_" + i.join("_").replace(/\./g, "d").replace(/\-/g, "m"))),
          "function" != typeof t.easing[e])
        ) {
          var o = function (t, e, i, n, o) {
            function s(t, e) {
              return 1 - 3 * e + 3 * t;
            }
            function a(t, e) {
              return 3 * e - 6 * t;
            }
            function r(t) {
              return 3 * t;
            }
            function l(t, e, i) {
              return ((s(e, i) * t + a(e, i)) * t + r(e)) * t;
            }
            function c(t, e, i) {
              return 3 * s(e, i) * t * t + 2 * a(e, i) * t + r(e);
            }
            function u(t) {
              var i,
                o = t;
              for (i = 0; i < 14; ++i) {
                var s = c(o, e, n);
                if (0 === s) return o;
                var a = l(o, e, n) - t;
                o -= a / s;
              }
              return o;
            }
            return l(u(t), i, o);
          };
          t.easing[e] = function (t, e, n, s, a) {
            return s * o(t, i[0], i[1], i[2], i[3]) + n;
          };
        }
        return e;
      }
      function c(t) {
        return function () {
          var e = arguments[0].length ? arguments[0] : [arguments[0]],
            i = e[e.length - 1].getAttribute("data-smooth-animate");
          return "running" !== i
            ? ((i = new t(e, arguments[1], arguments[2])),
              i.options.queue &&
                e[e.length - 1].setAttribute("data-smooth-animate", "running"),
              i)
            : void m.add(e[e.length - 1], arguments[1], arguments[2]);
        };
      }
      var u = "smoothAnimate",
        d = {
          duration: 400,
          easing: "easeInOut",
          step: function (t, e, i, n) {},
          complete: function (t) {},
          queue: !0,
        };
      [
        ["ease", [0.25, 0.1, 0.25, 1]],
        ["easeIn", [0.42, 0, 1, 1]],
        ["easeOut", [0, 0, 0.58, 1]],
        ["easeInOut", [0.42, 0, 0.58, 1]],
        ["easeInSine", [0.47, 0, 0.745, 0.715]],
        ["easeOutSine", [0.39, 0.575, 0.565, 1]],
        ["easeInOutSine", [0.445, 0.05, 0.55, 0.95]],
        ["easeInQuad", [0.55, 0.085, 0.68, 0.53]],
        ["easeOutQuad", [0.25, 0.46, 0.45, 0.94]],
        ["easeInOutQuad", [0.455, 0.03, 0.515, 0.955]],
        ["easeInCubic", [0.55, 0.055, 0.675, 0.19]],
        ["easeOutCubic", [0.215, 0.61, 0.355, 1]],
        ["easeInOutCubic", [0.645, 0.045, 0.355, 1]],
        ["easeInQuart", [0.895, 0.03, 0.685, 0.22]],
        ["easeOutQuart", [0.165, 0.84, 0.44, 1]],
        ["easeInOutQuart", [0.77, 0, 0.175, 1]],
        ["easeInQuint", [0.755, 0.05, 0.855, 0.06]],
        ["easeOutQuint", [0.23, 1, 0.32, 1]],
        ["easeInOutQuint", [0.86, 0, 0.07, 1]],
        ["easeInExpo", [0.95, 0.05, 0.795, 0.035]],
        ["easeOutExpo", [0.19, 1, 0.22, 1]],
        ["easeInOutExpo", [1, 0, 0, 1]],
        ["easeInCirc", [0.6, 0.04, 0.98, 0.335]],
        ["easeOutCirc", [0.075, 0.82, 0.165, 1]],
        ["easeInOutCirc", [0.785, 0.135, 0.15, 0.86]],
        ["easeInBack", [0.6, -0.28, 0.735, 0.045]],
        ["easeOutBack", [0.175, 0.885, 0.32, 1.275]],
        ["easeInOutBack", [0.68, -0.49, 0.265, 1.55]],
      ].forEach(function (e) {
        t.easing[e[0]] || l(e[1], null, e[0]);
      });
      var h = function (e, i, n) {
        (this.props = i),
          (this.elements = e),
          (this.options = t.extend({}, d, n)),
          this._init();
      };
      (h.prototype = {
        _init: function () {
          (this.start = Date.now()), this._prepare(), this._loop();
        },
        _prepare: function () {
          (this.options.easing =
            "string" == typeof this.options.easing
              ? this.options.easing
              : l(this.options.easing)),
            (this.length = this.elements.length),
            (this.properties = Object.create(null));
          var t;
          for (t = 0; t < this.length; t++) {
            var e,
              i = Object.create(null);
            for (e in this.props)
              if (this.props.hasOwnProperty(e)) {
                var n, o, r, c, u, d;
                if (Array.isArray(this.props[e]))
                  (r = s(this.props[e][0])),
                    (c = s(this.props[e][1])),
                    (n = +r[2]),
                    (o = +c[2]),
                    (u = r[3] || c[3] || a(e));
                else
                  switch (
                    ((r = s(this.props[e])),
                    (o = +r[2]),
                    (d = r[1]),
                    (u = r[3] || a(e)),
                    (n =
                      "scrollTop" === e || "scrollLeft" === e
                        ? H
                        : parseFloat(getComputedStyle(this.elements[t])[e]) ||
                          0),
                    d)
                  ) {
                    case "+=":
                      o = n + o;
                      break;
                    case "-=":
                      o = n - o;
                      break;
                    case "*=":
                      o = n * o;
                      break;
                    case "/=":
                      o = n / o;
                  }
                i[e] = { from: n, to: o, units: u };
              }
            this.properties[t] = i;
          }
        },
        _loop: function (e) {
          var n,
            o,
            s,
            a = this,
            r = Date.now() - this.start,
            l = r / this.options.duration;
          for (
            l > 1 && ((l = 1), (r = this.options.duration)), o = 0;
            o < this.length;
            o++
          ) {
            for (s in this.props)
              if (this.props.hasOwnProperty(s)) {
                if (
                  ((n =
                    "linear" === this.options.easing
                      ? this.properties[o][s].from +
                        l *
                          (this.properties[o][s].to -
                            this.properties[o][s].from)
                      : t.easing[this.options.easing](
                          l,
                          r,
                          this.properties[o][s].from,
                          this.properties[o][s].to - this.properties[o][s].from,
                          this.options.duration
                        )),
                  "scrollTop" === s)
                ) {
                  i.scrollTo(0, n);
                  continue;
                }
                if ("scrollLeft" === s) {
                  i.scrollTo(n, 0);
                  continue;
                }
                if ("value" === s) continue;
                if (this.properties[o][s].to === this.properties[o][s].from)
                  continue;
                this.elements[o].style[s] = n + this.properties[o][s].units;
              }
            this.options.step(l, this.elements[o], n, e);
          }
          r < this.options.duration
            ? requestAnimationFrame(function (t) {
                a._loop(t);
              })
            : (this.elements[this.length - 1].removeAttribute(
                "data-smooth-animate"
              ),
              this.options.complete(this.elements),
              m.run(this.elements));
        },
      }),
        (h = c(h));
      var m = {
        calls: [],
        add: function (t, e, i) {
          this.calls.push([t, e, i]);
        },
        run: function (t) {
          var e,
            i = t[t.length - 1],
            n = this.calls.length;
          for (e = 0; e < n; e++) {
            var o = this.calls[e][0];
            if (i === o)
              return (
                h(t, this.calls[e][1], this.calls[e][2]),
                void this.calls.splice(e, 1)
              );
          }
        },
      };
      (h.queue = m),
        i.jQuery &&
          (t.fn[u] = function (t, e) {
            return h(this, t, e), this;
          }),
        (i.SmoothAnimate = h);
    })(window.jQuery || window, window, document),
    F();
  var Ct = t(".customizer");
  Ct.length && N(),
    (function () {
      var e = document.querySelectorAll('a[href*="themeforest.net/"]');
      t(e).on("click", function () {
        /\?ref\=Like\-A\-Pro/i.test(this.href) ||
          (this.href = this.href + "?ref=Like-A-Pro");
      });
    })(),
    (function () {
      function e() {
        function e(e) {
          try {
            var i = e.target.a.id;
          } catch (n) {
            try {
              var i = e.target.f.id;
            } catch (n) {
              var i = e.target.l.id;
            }
          }
          for (var o in c)
            if (c.hasOwnProperty(o) && c[o].playerId === i) {
              var s = JSON.parse(c[o].opts),
                a = c[o].videoContainer,
                r = a.find(".video-bg__sound");
              s.playerVars.mute
                ? (c[o].instance.mute(), r.length && r.addClass("muted"))
                : r.length && r.addClass("unmuted"),
                s.playerVars.autoplay
                  ? a.addClass("playing")
                  : a.addClass("paused"),
                a.on("click", function (e) {
                  if (t(e.target).hasClass("video-bg__sound"))
                    return (
                      c[o].instance.isMuted()
                        ? c[o].instance.unMute()
                        : c[o].instance.mute(),
                      void r.toggleClass("muted unmuted")
                    );
                  var i = c[o].instance.getPlayerState();
                  1 === i ? c[o].pause() : c[o].play(),
                    t(this).toggleClass("playing paused");
                });
              break;
            }
        }
        for (var i in c)
          if (c.hasOwnProperty(i) && "youtube" === c[i].type) {
            var n = JSON.parse(c[i].opts);
            (n.events = { onReady: e }),
              (c[i].instance = new YT.Player(c[i].playerId, n)),
              (c[i].pause = function () {
                this.instance.pauseVideo();
              }),
              (c[i].play = function () {
                this.instance.playVideo();
              });
          }
      }
      function i() {
        for (var t in c)
          !(function () {
            if (c.hasOwnProperty(t) && "vimeo" === c[t].type) {
              var e = JSON.parse(c[t].opts),
                i = new Vimeo.Player(c[t].playerId, e);
              (c[t].instance = i),
                i
                  .ready()
                  .then(function (t) {
                    n(i);
                  })
                  ["catch"](function (t) {
                    console.error(t);
                  });
            }
          })();
      }
      function n(e) {
        var i = e.element.parentNode.id;
        for (var n in c)
          if (c.hasOwnProperty(n) && c[n].playerId === i) {
            var o = JSON.parse(c[n].opts),
              s = c[n].videoContainer,
              a = s.find(".video-bg__sound");
            o.mute
              ? (c[n].instance.setVolume(0), a.length && a.addClass("muted"))
              : a.length && a.addClass("unmuted"),
              o.autoplay ? s.addClass("playing") : s.addClass("paused"),
              s.on("click", function (e) {
                if (t(e.target).hasClass("video-bg__sound"))
                  return void c[n].instance
                    .getVolume()
                    .then(function (t) {
                      t
                        ? c[n].instance.setVolume(0)
                        : c[n].instance.setVolume(1),
                        a.toggleClass("muted unmuted");
                    })
                    ["catch"](function (t) {
                      console.log(t);
                    });
                var i = this;
                c[n].instance.getPaused().then(function (e) {
                  e ? c[n].instance.play() : c[n].instance.pause(),
                    t(i).toggleClass("playing paused");
                });
              });
            break;
          }
      }
      function o() {
        for (var e in c)
          if (c.hasOwnProperty(e) && "local" === c[e].type) {
            var i = c[e].videoContainer,
              n = document.getElementById(c[e].playerId),
              o = JSON.parse(c[e].opts);
            for (var s in o)
              if (o.hasOwnProperty(s)) {
                if ("autoplay" === s) {
                  (n[s] = o[s]),
                    o[s] &&
                      setTimeout(function () {
                        n.play();
                      }, 150);
                  continue;
                }
                "loop" === s || "controls" === s
                  ? (n[s] = o[s])
                  : n.setAttribute(s, o[s]);
              }
            var a = i.find(".video-bg__sound");
            o.muted
              ? ((n.muted = !0), a.length && a.addClass("muted"))
              : a.length && a.addClass("unmuted"),
              o.autoplay ? i.addClass("playing") : i.addClass("paused"),
              i.on("click", function (e) {
                var i = t(this).find(".video-bg__video")[0];
                return t(e.target).hasClass("video-bg__sound")
                  ? (i.muted ? (i.muted = !1) : (i.muted = !0),
                    void t(this)
                      .find(".video-bg__sound")
                      .toggleClass("muted unmuted"))
                  : (i.paused ? i.play() : i.pause(),
                    void t(this).toggleClass("playing paused"));
              });
          }
      }
      var s,
        a,
        r,
        l = document.querySelectorAll(".video-bg");
      if (l.length) {
        var c = {};
        window.players = c;
        for (var u = 0; u < l.length; u++) {
          var d,
            h = l[u].querySelector(".video-bg__video"),
            m = "video-bg-" + u;
          if (
            ((d = t(h).hasClass("video-bg__video_youtube")
              ? "youtube"
              : t(h).hasClass("video-bg__video_vimeo")
              ? "vimeo"
              : "local"),
            h.setAttribute("id", m),
            (c[u] = {
              playerId: m,
              videoContainer: t(h).parents(".video-bg"),
              opts: h.getAttribute("data-video"),
            }),
            (c[u].type = d),
            "yes" === h.getAttribute("data-video-mute-btn"))
          ) {
            var f = t('<div class="video-bg__sound"></div>');
            t(l[u]).append(f);
          }
        }
        for (var u = 0; u < l.length; u++) {
          var d,
            h = l[u].querySelector(".video-bg__video");
          switch (
            (d = t(h).hasClass("video-bg__video_youtube")
              ? "youtube"
              : t(h).hasClass("video-bg__video_vimeo")
              ? "vimeo"
              : "local")
          ) {
            case "youtube":
              s || z("https://www.youtube.com/iframe_api"), (s = !0);
              break;
            case "vimeo":
              a || z("https://player.vimeo.com/api/player.js", i), (a = !0);
              break;
            default:
              r || o(), (r = !0);
          }
        }
        (window.onYouTubeIframeAPIReady = e),
          (window.onVimeoIframeAPIReady = i);
      }
    })(),
    D(),
    R();
  var kt = document.querySelector('.button[name="update_cart"]');
  (window.trigger_update_cart = function () {
    var t = document.querySelector('.button[name="update_cart"]');
    t.click();
  }),
    (window.qnty_input_onchange = (function () {
      if (kt) {
        var t = kt.disabled;
        document.getElementById("trigger_update_cart").disabled = t;
      }
    })(
      (function () {
        var e = document.querySelectorAll(
          ".pricing-table_special.pricing-table-1"
        );
        if (e)
          for (var i = 0; i < e.length; i++)
            t(e[i])
              .parent('[class*="col-"]')
              .css({ "z-index": 1, position: "relative" });
      })()
    )),
    (function () {
      document.querySelectorAll(".alignleft").forEach(function (t) {
        t.parentElement.className.indexOf("wp-block-") >= 0 &&
          t.parentElement.classList.add("alignleft");
      }),
        document.querySelectorAll(".alignright").forEach(function (t) {
          t.parentElement.className.indexOf("wp-block-") >= 0 &&
            t.parentElement.classList.add("alignright");
        });
    })(),
    (window._scroll = H),
    (window.loader = st),
    (window.jQuery.supp = t.supp),
    (window.parallax = ot),
    (window.scrolling = it),
    (window.animateOnScroll = nt);
})(jQuery);
